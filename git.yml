Quick changes in setup — if you’ve done this kind of thing before


git clone https://github.com/jaysin092/ajay.git


…or create a new repository on the command line

echo "# ajay" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/jaysin092/ajay.git
git push -u origin main


…or push an existing repository from the command line

git remote add origin https://github.com/jaysin092/ajay.git
git branch -M main
git push -u origin main


